jQuery ->
  $("a[rel=popover]").popover()
  $(".tooltip").tooltip()
  $("a[rel=tooltip]").tooltip()
  $('.carousel').carousel({ 
	  interval: 3000 
      'cycle'
	  })

  
  $('#myModal').modal({
    backdrop:true,
    keyboard:true,
    show:true
    });
