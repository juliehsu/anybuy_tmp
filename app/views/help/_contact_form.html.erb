  
  <%= f.input :username, label: '姓名', placeholder: "#{current_user.username}" %> 

  <%= f.input :phone, label:'聯絡電話', placeholder: "#{current_user.phone}" %> 

  <%= f.input :email, label:'Email', placeholder: "#{current_user.email}"%> 

  <%= f.input :contact_message, label: '內容'  %>
